import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ProfileScreen() {
  return <ScreenPlaceholder title="Profil" description="La gestion du profil sera ajoutée dans le socle de la semaine 1." />;
}
